import classNames from "classnames/bind";
import styles from "./AddStoreForm.module.scss"

const cx = classNames.bind(styles);

export const AddStoreForm = () => {
    return (
        <h3>Danh sach cu hang ne</h3>
    )
}