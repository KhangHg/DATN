import classNames from 'classnames/bind';
import styles from './OrderList.module.scss';
const cx = classNames.bind(styles);

const OrderList = () => {
    return (
        <div>
            Trang danh sách đơn hàng của admin
        </div>
    )
}

export default OrderList;